console.log("page loaded...");

function playVideo(vid) {
    vid.play();
}

function pauseVideo(vid) {
    vid.pause();
    vid.currentTime = 0;
}

function playVideoP(vid) {
    vid.play();
}

function pauseVideoP(vid) {
    vid.pause();
    vid.currentTime = 0;
}